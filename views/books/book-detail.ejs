<%- include('../partials/header.ejs') %>
    </head>


    <body>
        <%- include('../partials/navigation.ejs') %>
        <main class="centered">
            <h1><%= book.title %></h1>
            <hr>
            <div class="image">
                <img src="/<%= book.imageUrl %>" alt="<%= book.title %>">
            </div>
            <h2><%= book.price %> kn</h2>
            <p><%= book.description %></p>
            <% if (currentUser) { %>
            <%if (currentUser.status === "admin") { %> 
                <div class="card__actions">
                    <a href="/edit-book/<%= book._id %>?edit=true" class="btn">Edit</a>
                    <input type="hidden" value="<%= book._id %>" name="bookId">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <form class="product-form" action="/delete-book/<%= book._id %>" method="GET" enctype="multipart/form-data">
                        <button class="btn">Delete</button>
                    </form>
                </div>
            <% } %>
            <% } %>
            <div class="col-12">
                </div>
               <main>
                    <% if (relatedBooks.length > 1) { %>
                   <h2 class="centered"> Related products </h2>
                       <div class="grid">
                           <% for (let rb of relatedBooks) { %>
                            <% if(rb.title != book.title) {%>
                               <article class="card product-item">
                                   <header class="card__header">
                                       <h1 class="product__title">                                        
                                           <%= rb.title %>
                                       </h1>
                                   </header>
                                   <div class="card__image">
                                       <img src="/<%= rb.imageUrl %>" alt="<%= rb.title %>">
                                   </div>
                                   <div class="card__content">
                                       <h2 class="product__price">
                                           <%= rb.price %> kn
                                       </h2>
                                       <p class="product__description">
                                           <%= rb.description %>
                                       </p>
                                   </div>
                                   <div class="card__actions">
                                       <a href="/books/<%= rb._id %>" class="btn">Details</a>
                                       </div>
                                      
                               </article>
                               <% } %>
                               <% } %>
                               
                       </div>
                       <% } %>
        </main>
        <%- include('../partials/end.ejs') %>